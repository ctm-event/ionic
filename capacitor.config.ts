import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.steven.firstApp',
  appName: 'First App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
